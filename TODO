L2517: vendor/beberlei/assert/lib/Assert/Assertion.php
if (\strlen($val) > 100) {
                $val = \substr($val, 0, 97) . '...';
            }

This is silly, should rely on log_errors_max_len instead


add validation in stub generator
check that the options make sense
more user-friendly errors when the config file is invalid
